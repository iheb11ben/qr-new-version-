const NotFoundError = require("./not-found");
const BadRequestError = require("./bad-request");
const CustomAPIError = require("./custom-api");

module.exports = { NotFoundError, BadRequestError, CustomAPIError };
